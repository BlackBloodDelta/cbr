import React from 'react';

const BlogTitle = () => {
    return (
        <div className={"blog-header"}>
            <h1 className="text-center">Veja os posts mais recentes</h1>
        </div>
    );
};

export default BlogTitle;
